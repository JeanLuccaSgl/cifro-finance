import Home from "../page";

export const metadata = {
  title: "Simulador — Cifro",
  description: "Compare cenários financeiros sem alterar seus registros reais.",
};

export default function SimulatorPage() {
  return <Home view="simulator" />;
}
