import Home from "../page";

export const metadata = {
  title: "Registrar — Cifro",
  description: "Registre uma nova movimentação no Cifro.",
};

export default function RegisterPage() {
  return <Home view="register" />;
}
